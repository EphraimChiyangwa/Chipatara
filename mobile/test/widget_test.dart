import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App launches smoke test', (WidgetTester tester) async {
    // Basic smoke test — app entry point tested separately via integration tests
    expect(true, isTrue);
  });
}
